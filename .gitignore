/bin/
/tmp/
/data/
/xilo.yaml
/result
.direnv/
*.db
*.db-wal
*.db-shm

# templ output — generated at build time, never committed
*_templ.go

.env

# Tailwind machine-specific @source globs (regenerated by `just css`)
internal/server/views/css/sources.generated.css
internal/server/static/xilo-tw.css

# Compiled test binaries (`go test -c`) — one got committed once
*.test
